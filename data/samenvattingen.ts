/* Beknopte theorie-samenvatting per labo, getoond bovenaan elk hoofdstuk op de
   /theorie-pagina. Elke sectie heeft een titel (h) en een lijst uitlegpunten
   (points). Zowel h als de punten zijn HTML-strings: inline code staat in
   <code>...</code>, en letterlijke < > & zijn ge-escaped (&lt; &gt; &amp;). */
export interface SummarySection {
  h: string;
  points: string[];
}

export const SUMMARIES: Record<string, SummarySection[]> = {
  labo13: [
    { h: "Setup & het load-event", points: ["Je zet je code in <code>const setup = () =&gt; { ... }</code> en koppelt ze met <code>window.addEventListener(\"load\", setup)</code>, zodat ze pas draait wanneer de volledige pagina is ingeladen en alle HTML-elementen bestaan.", "Je geeft <code>setup</code> mee <b>zonder</b> haakjes: je geeft de functie door, je roept ze niet zelf op.", "Anders zou je script al kunnen lopen voordat de elementen op de pagina bestaan, en kan je ze nog niet ophalen of aanpassen."] },
    { h: "Variabelen: let &amp; const", points: ["Je declareert variabelen met <code>let</code> (waarde mag later wijzigen) of <code>const</code> (constante, kan na de eerste toewijzing niet meer opnieuw toegewezen worden).", "Beide zijn block-scoped: ze bestaan enkel in het blok <code>{ ... }</code> waarin je ze declareert.", "Je gebruikt <b>nooit</b> <code>var</code>: dat zorgt voor verwarrende scoping en subtiele fouten.", "Een lokale variabele kent enkel de functie waarin ze staat; een globale variabele is overal bekend. Je beperkt globale variabelen, want twee scripts kunnen per ongeluk dezelfde naam overschrijven."] },
    { h: "Console &amp; dialoogvensters", points: ["Met <code>console.log(\"...\")</code> schrijf je iets naar de console om snel te testen; <code>console.error(\"...\")</code> toont een foutmelding. De console is een hulpmiddel voor jou, niet zichtbaar voor de gebruiker.", "<code>prompt(\"Vraag?\")</code> opent een invoervenster en geeft de ingetypte tekst terug als string, of <code>null</code> als de gebruiker op Annuleren klikt.", "<code>alert(\"Bericht\")</code> toont een mededeling; <code>confirm(\"Zeker?\")</code> geeft <code>true</code> (OK) of <code>false</code> (Annuleren) terug."] },
    { h: "Arrow functions", points: ["Je schrijft functies altijd als arrow function en kent ze toe aan een <code>const</code>: <code>const dubbel = (x) =&gt; { return x * 2; };</code>.", "Bestaat de functie uit één enkele uitdrukking, dan mag je de accolades en <code>return</code> weglaten (impliciete return): <code>const dubbel = (x) =&gt; x * 2;</code>.", "De oude schrijfwijze met <code>function</code> gebruik je in deze cursus niet."] },
    { h: "Beslissingen: if &amp; vergelijken", points: ["Met <code>if (...) { ... } else if (...) { ... } else { ... }</code> laat je code afhangen van een voorwaarde.", "Je vergelijkt met <code>===</code> (gelijk, ook het type telt) en <code>!==</code> (niet gelijk), en met <code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>. Gebruik altijd <code>===</code>, nooit <code>==</code>.", "Voorwaarden combineer je met <code>&amp;&amp;</code> (en), <code>||</code> (of) en <code>!</code> (niet).", "Een korte keuze schrijf je met de ternary: <code>const tekst = (uur &lt; 12) ? \"voor\" : \"na\";</code> (voorwaarde ? alsWaar : alsOnwaar)."] },
    { h: "switch &amp; lussen", points: ["Met <code>switch (waarde) { case 1: ...; break; default: ...; }</code> kies je tussen vaste mogelijkheden; vergeet de <code>break;</code> per case niet.", "De klassieke lus herhaal je een vast aantal keer: <code>for (let i = 0; i &lt; 5; i++) { ... }</code>.", "Met <code>for (const item of lijst) { ... }</code> overloop je rechtstreeks de waarden, en met <code>lijst.forEach((item) =&gt; { ... })</code> doe je hetzelfde via een functie.", "Met <code>while (voorwaarde) { ... }</code> blijf je herhalen zolang de voorwaarde waar is."] },
  ],
  labo14: [
    { h: "Arrays maken &amp; uitlezen", points: ["Een array is een geordende lijst van waarden: <code>const fruit = [\"appel\", \"peer\", \"banaan\"];</code>.", "Je telt de elementen met de property <code>fruit.length</code> (geen functie, dus zonder haakjes).", "Je indexeert vanaf <code>0</code>: <code>fruit[0]</code> is het eerste element, <code>fruit[2]</code> het derde. Een array werkt ook prima met <code>const</code>, want je wijzigt de inhoud, niet de variabele zelf."] },
    { h: "Elementen toevoegen &amp; verwijderen", points: ["<code>fruit.push(\"kers\")</code> voegt achteraan toe, <code>fruit.pop()</code> verwijdert het laatste element.", "<code>fruit.unshift(\"aardbei\")</code> voegt vooraan toe, <code>fruit.shift()</code> verwijdert het eerste element.", "Met <code>fruit.splice(1, 1)</code> verwijder je gericht elementen (hier 1 element vanaf index 1).", "Omdat een array via referentie wordt doorgegeven, blijft een wijziging zoals <code>push</code> ook zichtbaar in de oorspronkelijke array buiten je functie."] },
    { h: "Zoeken in een array", points: ["<code>fruit.indexOf(\"peer\")</code> geeft de positie (index) van het eerste voorkomen terug, of <code>-1</code> als het er niet in zit.", "<code>fruit.lastIndexOf(\"peer\")</code> geeft de positie van het láátste voorkomen terug.", "Zo kan je met <code>if (fruit.indexOf(\"peer\") !== -1) { ... }</code> nagaan of iets in de array zit."] },
    { h: "innerHTML vs. textContent", points: ["Je haalt een element op met <code>document.getElementById(\"id\")</code> en past dan zijn inhoud aan.", "Met <code>element.innerHTML = \"&lt;b&gt;vet&lt;/b&gt;\"</code> zet je inhoud waarin HTML-tags ook écht als opmaak verwerkt worden.", "Met <code>element.textContent = \"tekst\"</code> zet je platte tekst; eventuele tags worden letterlijk getoond en niet geïnterpreteerd.", "De ingetypte waarde van een invoerveld lees je uit met <code>element.value</code>."] },
    { h: "Strings bewerken", points: ["Net als bij een array geeft <code>tekst.length</code> de lengte en haal je een teken op via indexering: <code>tekst[0]</code> of <code>tekst.charAt(0)</code> is het eerste teken (vanaf index 0).", "<code>tekst.substring(2, 5)</code> en <code>tekst.slice(2, 5)</code> knippen het deel van index 2 tot (maar niet inclusief) index 5.", "<code>tekst.indexOf(\"a\")</code> geeft de positie van een teken of stuk tekst (of <code>-1</code>).", "<code>tekst.toUpperCase()</code> en <code>tekst.toLowerCase()</code> zetten alles in hoofd- of kleine letters; <code>tekst.trim()</code> knipt spaties vooraan en achteraan weg."] },
  ],
  labo15: [
    { h: "Reageren op events", points: ["Je laat code lopen als reactie op een gebeurtenis met <code>element.addEventListener(\"click\", functie)</code>. Veelgebruikte events zijn <code>\"click\"</code> (klikken), <code>\"input\"</code> (typen of schuiven) en <code>\"change\"</code> (waarde gewijzigd en verlaten).", "Je geeft de functienaam mee <b>zonder</b> haakjes: <code>addEventListener(\"click\", update)</code>. Met haakjes (<code>update()</code>) roep je de functie meteen één keer op in plaats van ze pas bij het event te laten draaien.", "Een schuifbalk koppel je vaak aan zowel <code>\"input\"</code> als <code>\"change\"</code>, omdat browsers die events anders behandelen."] },
    { h: "Elementen selecteren", points: ["<code>document.getElementById(\"id\")</code> geeft het ene element met dat unieke id terug.", "<code>document.getElementsByClassName(\"klasse\")</code> en <code>document.getElementsByTagName(\"p\")</code> geven een <code>HTMLCollection</code> van álle passende elementen terug; één element haal je eruit met <code>[0]</code>.", "<code>document.querySelector(\".klasse\")</code> geeft het eerste element dat bij de CSS-selector past, <code>document.querySelectorAll(\"p\")</code> geeft ze allemaal."] },
    { h: "Een HTMLCollection overlopen", points: ["Een <code>HTMLCollection</code> overloop je net als een array met een <code>for</code>-lus: <code>for (let i = 0; i &lt; lijst.length; i++) { ... }</code>.", "Je spreekt elk element aan met <code>lijst[i]</code> en past het binnenin aan.", "Je gebruikt <code>.length</code> zodat je code blijft werken ongeacht hoeveel elementen er zijn: je hardcodeert het aantal niet."] },
    { h: "CSS aanpassen via JavaScript", points: ["Je zet een losse CSS-eigenschap rechtstreeks met <code>element.style</code>, bv. <code>element.style.backgroundColor = \"red\"</code> of <code>element.style.fontSize = \"20px\"</code>.", "Een kleur bouw je handig met een template-string: <code>element.style.backgroundColor = `rgb(${r}, ${g}, ${b})`;</code>.", "Liever werk je met classes uit je CSS-bestand: <code>element.className = \"actief\"</code> vervangt alle classes, terwijl <code>element.classList.add(\"actief\")</code>, <code>.remove(\"actief\")</code> en <code>.toggle(\"actief\")</code> er één toevoegen, weghalen of omschakelen.", "Je kiest <code>style</code> wanneer er te veel mogelijke waarden zijn voor een aparte class (bv. een vrij gekozen kleur), en anders een class."] },
    { h: "Rekenen met Number", points: ["Een ingelezen <code>value</code> of <code>textContent</code> is tekst; om ermee te rekenen zet je die eerst om naar een getal.", "<code>parseInt(\"42px\", 10)</code> leest een geheel getal (geef altijd radix <code>10</code> mee), <code>parseFloat(\"3.14 Eur\")</code> leest een kommagetal en negeert de rest, en <code>Number(\"42\")</code> zet een nette tekst om.", "<code>(3.14159).toFixed(2)</code> rondt af op 2 cijfers na de komma, maar geeft een <b>string</b> terug (bv. <code>\"3.14\"</code>) — handig om bedragen te tonen.", "Met <code>isNaN(...)</code> controleer je of iets géén geldig getal is, en met de restoperator <code>getal % 2 === 0</code> test je of een getal even is."] },
  ],
  labo16: [
    { h: "HTML-formulieren opbouwen", points: ["Een formulier zet je in een <code>&lt;form&gt;</code> met een <code>action</code> (waar de data heen gaat) en een <code>method</code>; gebruik <code>method=\"post\"</code> om gegevens op te slaan (schrijven) en <code>method=\"get\"</code> om te lezen — bij <code>get</code> komt de data zichtbaar in de URL.", "Het <code>name</code>-attribuut is de sleutel waaronder een veld verstuurd wordt (bv. <code>postcode=8500</code>); een <code>id</code> moet uniek zijn op de pagina, maar eenzelfde <code>name</code> mag meermaals voorkomen (denk aan radioknoppen in één groep).", "Koppel een <code>&lt;label&gt;</code> aan een veld via <code>for</code> (gelijk aan de <code>id</code> van het veld): een klik op het label activeert dan het veld zelf.", "Met <code>required</code> moet een veld ingevuld zijn en met <code>autofocus</code> staat de cursor er meteen in; een verzendknop maak je met <code>&lt;input type=\"submit\"&gt;</code>."] },
    { h: "Formuliervelden uitlezen in JavaScript", points: ["Een gewoon tekstveld lees je met <code>element.value</code> (altijd een string).", "Voor een checkbox of een radioknop gebruik je <code>element.checked</code>: dat geeft <code>true</code> of <code>false</code> terug.", "Een hele radiogroep haal je op met <code>document.getElementsByName(\"naam\")</code>; daarna overloop je de groep en lees je de <code>.value</code> van het element waar <code>.checked</code> gelijk is aan <code>true</code>.", "Bij een keuzelijst geeft <code>select.value</code> de gekozen waarde, <code>select.selectedIndex</code> de gekozen positie, en <code>select.options[i].text</code> de zichtbare tekst van optie <code>i</code>."] },
    { h: "Het type opvragen met typeof", points: ["Met <code>typeof waarde</code> vraag je het type op; het resultaat is zelf een string, zoals <code>\"number\"</code>, <code>\"string\"</code> of <code>\"boolean\"</code>.", "Er bestaan minder types dan je zou denken: <code>typeof 42</code> en <code>typeof 0.12</code> geven allebei <code>\"number\"</code> (geen apart type voor kommagetallen).", "Let op de eigenaardigheden: <code>typeof null</code> geeft <code>\"object\"</code>, en ook <code>typeof []</code> en <code>typeof new Date()</code> geven <code>\"object\"</code> — een array of een datum is dus geen apart type."] },
    { h: "Tekst splitsen en samenvoegen", points: ["Met <code>tekst.split(scheidingsteken)</code> hak je een string in een array van stukken; <code>\"a,b,c\".split(\",\")</code> geeft <code>['a', 'b', 'c']</code>, en een lege string <code>split(\"\")</code> splitst per karakter.", "Het omgekeerde doe je met <code>array.join(verbinder)</code>: dat plakt de stukken weer aaneen, bv. <code>['a', 'b'].join(\" - \")</code> geeft <code>\"a - b\"</code>.", "Met <code>tekst.replaceAll(zoek, vervang)</code> vervang je ALLE voorkomens van een stuk tekst; <code>tekst.includes(zoek)</code> geeft <code>true</code> of <code>false</code> naargelang de tekst erin voorkomt."] },
    { h: "Template literals en string-methoden", points: ["Een template literal schrijf je tussen backticks <code>`...`</code> en laat je toe variabelen in te voegen met <code>${...}</code>, bv. <code>`an komt ${aantal} keer voor`</code> — handiger dan losse stukken met <code>+</code> aaneen te plakken.", "Met <code>tekst.indexOf(zoek)</code> vind je de positie van de eerste treffer (vanaf vooraan) en met <code>tekst.lastIndexOf(zoek)</code> die van de laatste (vanaf achteraan); beide geven <code>-1</code> als de tekst niet voorkomt.", "Met <code>tekst.substring(start, eind)</code> knip je een deeltekst uit, waarbij de eindindex \"tot\" (niet \"tot en met\") betekent; <code>tekst.trim()</code> verwijdert witruimte vooraan en achteraan, <code>toUpperCase()</code> en <code>toLowerCase()</code> wijzigen de hoofdletters.", "Je mag methoden aaneenketenen, bv. <code>tekst.trim().toUpperCase()</code>."] },
  ],
  labo17: [
    { h: "Deelteksten met slice", points: ["Met <code>tekst.slice(start, eind)</code> haal je een deeltekst op; de rechtergrens is NIET inbegrepen, dus <code>\"Hello world\".slice(3, 8)</code> geeft <code>\"lo wo\"</code> (karakters op index 3 t/m 7).", "De cursus raadt <code>slice</code> aan boven <code>substring</code>; <code>slice</code> kan bovendien overweg met negatieve indexen om vanaf het einde te tellen.", "Een deeltekst van vaste lengte in een lus haal je op met <code>tekst.slice(i, i + 3)</code>, bijvoorbeeld om alle opeenvolgende stukjes van drie letters te overlopen."] },
    { h: "Strings vergelijken", points: ["Voor \"exact gelijk\" gebruik je <code>===</code>: <code>\"appel\" === \"appel\"</code> geeft <code>true</code>. Gebruik altijd <code>===</code> en nooit <code>==</code>.", "Voor de alfabetische volgorde gebruik je <code>s1.localeCompare(s2)</code>, NIET de operatoren <code>&lt;</code> of <code>&gt;</code> (die werken op ASCII-code en zetten bv. hoofdletters vóór kleine letters).", "<code>localeCompare</code> geeft GEEN boolean maar een getal: kleiner dan 0 als <code>s1</code> vóór <code>s2</code> komt, 0 als ze gelijk zijn, en groter dan 0 als <code>s1</code> na <code>s2</code> komt."] },
    { h: "Arrays sorteren met een vergelijkingsfunctie", points: ["Je geeft <code>sort</code> een vergelijkingsfunctie <code>(a, b) =&gt; ...</code> mee die negatief teruggeeft als <code>a</code> vóór <code>b</code> moet komen, 0 bij gelijk, en positief als <code>a</code> na <code>b</code> moet komen.", "Getallen sorteer je oplopend met <code>array.sort((a, b) =&gt; a - b)</code> en aflopend met <code>array.sort((a, b) =&gt; b - a)</code>.", "Tekst sorteer je alfabetisch met <code>array.sort((a, b) =&gt; a.localeCompare(b))</code>.", "Belangrijk: <code>sort</code> wijzigt het ORIGINELE array. Wil je dat sparen, maak dan eerst een kopie met <code>array.slice()</code> en sorteer die kopie."] },
    { h: "Getallen uit een formulier", points: ["<code>element.value</code> levert ALTIJD een string op, ook bij een veld met <code>type=\"number\"</code>.", "Wil je ermee rekenen, zet de waarde dan eerst om naar een getal met <code>Number(...)</code> of <code>parseInt(...)</code>."] },
    { h: "Formuliervalidatie-patroon", points: ["Lees de waarde uit en verwijder meteen overbodige witruimte met <code>.trim()</code>, bv. <code>const waarde = veld.value.trim();</code>.", "Controleer op leeg met <code>waarde === \"\"</code>; is het veld leeg, toon dan een foutmelding en stop de verwerking met <code>return</code>.", "Voor strengere controles overloop je de tekst teken per teken (bv. <code>waarde[i] === \"@\"</code>) en tel of toets je de posities, in plaats van de invoer zomaar te aanvaarden."] },
  ],
  labo18: [
    { h: "DOM-nodes aanmaken, toevoegen en verwijderen", points: ["Een nieuw element maak je met <code>document.createElement(\"p\")</code> en een los stukje tekst met <code>document.createTextNode(\"tekst\")</code>; geef een element tekst via <code>element.textContent = \"...\"</code>.", "Toevoegen doe je met <code>ouder.appendChild(nieuw)</code> (achteraan) of <code>ouder.insertBefore(nieuw, referentie)</code> (vóór een bestaand kind).", "Verwijderen doe je met <code>ouder.removeChild(kind)</code>.", "Voordeel boven <code>.innerHTML</code>: je vervangt niet álle kinderen (en verliest geen bestaande verwijzingen of listeners), en je houdt een verwijzing naar het nieuwe element over."] },
    { h: "Navigeren door de DOM-tree", points: ["Omhoog ga je met <code>element.parentNode</code> (het ouder-element).", "Naar de kinderen ga je met <code>element.children</code> (enkel element-kinderen), of met <code>element.firstElementChild</code> en <code>element.lastElementChild</code> voor het eerste of laatste.", "Naar buren ga je met <code>element.nextElementSibling</code> en <code>element.previousElementSibling</code>.", "Met <code>element.childNodes</code> krijg je ALLE nodes, ook tekst-nodes: <code>nodeType</code> is <code>1</code> voor een element en <code>3</code> voor een tekst-node; <code>nodeName</code> geeft de tagnaam in hoofdletters (bv. <code>\"LI\"</code>) en <code>nodeValue</code> de tekst van een tekst-node."] },
    { h: "Attributen lezen en zetten", points: ["Een attribuut lees je met <code>element.getAttribute(\"src\")</code> en stel je in met <code>element.setAttribute(\"src\", \"foto.jpg\")</code>.", "Met <code>element.hasAttribute(\"disabled\")</code> test je of een attribuut aanwezig is (<code>true</code>/<code>false</code>).", "Eigen gegevens hang je aan een element via een <code>data-</code>attribuut, bv. <code>element.setAttribute(\"data-id\", \"5\")</code>."] },
    { h: "Het event-object", points: ["Elke listener krijgt automatisch een <code>event</code>-object mee, bv. <code>(event) =&gt; { ... }</code>.", "<code>event.target</code> is het element waar het event ONTSTOND (bv. waarop geklikt werd); <code>event.currentTarget</code> is het element waaraan de listener gekoppeld is. Vaak zijn ze gelijk, maar niet altijd.", "Met <code>event.preventDefault()</code> stop je het standaardgedrag, bv. zodat een formulier niet effectief verstuurt."] },
    { h: "Event bubbling en delegation", points: ["Een event borrelt na het ontstaan omhoog door de DOM-tree (kind → ouder → ... ), waardoor ook listeners op voorouders de kans krijgen te reageren.", "Daardoor kan je met één enkele listener op de OUDER alle kliks op de kinderen opvangen (\"event delegation\"), in plaats van een listener per kind.", "In zo'n gedeelde listener gebruik je <code>event.target</code> om te weten op welk kind precies geklikt werd, bv. een controle <code>event.target.nodeName === \"IMG\"</code> vóór je iets met dat element doet."] },
    { h: "Het this-keyword in listeners", points: ["In een GEWONE function-listener verwijst <code>this</code> naar het element dat het event ontving, bv. <code>this.style.color = \"red\"</code> kleurt de aangeklikte knop.", "Een arrow function heeft GEEN eigen <code>this</code>: daar verwijst <code>this</code> naar de omringende (buitenste) scope, bv. het <code>Window</code>-object, en dus niet naar het element.", "Om verwarring te vermijden gebruik je in een listener best <code>event.currentTarget</code> (of <code>event.target</code>): dat geeft altijd het juiste element, ongeacht het soort functie."] },
  ],
  labo19: [
    { h: "Timers: herhalen vs. één keer", points: ["<code>setInterval(fn, 1000)</code> roept <code>fn</code> telkens opnieuw op, om de zoveel milliseconden (hier elke seconde).", "<code>setTimeout(fn, 2000)</code> voert <code>fn</code> daarentegen exact één keer uit, na de opgegeven wachttijd.", "Beide geven een <code>id</code> terug: bewaar dat (bv. <code>let timerId = setInterval(...)</code>) zodat je de timer later kunt stoppen.", "Stoppen doe je met <code>clearInterval(id)</code> (bij een interval) of <code>clearTimeout(id)</code> (bij een timeout)."] },
    { h: "Duurtijd meten", points: ["<code>console.time(\"label\")</code> start een chronometer; <code>console.timeEnd(\"label\")</code> stopt ze en logt meteen het verstreken tijdsverschil.", "Gebruik bij beide exact hetzelfde label, anders kan de browser start en stop niet koppelen.", "Dit is handiger én nauwkeuriger dan zelf met <code>Date.now()</code> een start- en eindtijd af te trekken."] },
    { h: "Willekeurige getallen met Math.random()", points: ["<code>Math.random()</code> geeft een kommagetal in <code>[0, 1[</code> (0 inclusief, 1 exclusief).", "<code>Math.floor(x)</code> rondt naar beneden af, <code>Math.ceil(x)</code> naar boven, en <code>Math.round(x)</code> naar het dichtstbijzijnde gehele getal.", "Een willekeurig kommagetal in <code>[0, n[</code> krijg je met <code>Math.random() * n</code>; met <code>Math.floor</code> errond maak je er een geheel getal van."] },
    { h: "Een geheel getal in een bereik", points: ["Voor een willekeurig geheel getal in een bereik gebruik je de formule <code>Math.floor(Math.random() * (max - min + 1)) + min</code> (met <code>min</code> en <code>max</code> beide inclusief).", "Wil je <code>max</code> juist NIET meetellen, laat dan de <code>+ 1</code> weg: <code>Math.floor(Math.random() * (max - min)) + min</code>.", "<code>Math.floor</code> (en niet <code>Math.round</code>) gebruik je hier, want <code>Math.round</code> zou de grenswaarden oneerlijk vaak opleveren."] },
    { h: "Globale variabelen bundelen", points: ["Losse globale variabelen worden eigenschappen van het <code>window</code>-object en geven zo snel naamconflicten.", "Bundel je gegevens daarom in één object, bv. <code>const global = { score: 0, naam: \"speler1\" };</code>, en spreek ze aan via <code>global.score</code>.", "Zo vervuil je de global namespace veel minder (slechts één globale naam) en vind je al je data makkelijk terug bij het debuggen.", "Constanten schrijf je vaak in HOOFDLETTERS (bv. <code>MAX_SCORE: 100</code>) zodat ze opvallen tussen de gewone eigenschappen."] },
  ],
  labo20: [
    { h: "Een Date-object maken", points: ["<code>new Date()</code> geeft de huidige datum en tijd op het moment dat je de regel uitvoert.", "<code>new Date(jaar, maand, dag)</code> maakt een specifieke datum — let op: de <code>maand</code> is 0-gebaseerd, dus <code>0</code> = januari en <code>11</code> = december.", "<code>new Date(2025, 0, 1)</code> is dus 1 januari 2025, NIET 1 februari."] },
    { h: "Velden uit een Date lezen", points: ["<code>getFullYear()</code> geeft het jaar, <code>getDate()</code> de dag van de maand (1–31) en <code>getDay()</code> de weekdag (0 = zondag).", "<code>getMonth()</code> begint bij 0, dus tel er <code>+ 1</code> bij op als je de maand voor een mens wil tonen.", "<code>getHours()</code> (0–23), <code>getMinutes()</code> en <code>getSeconds()</code> geven de tijd.", "<code>getTime()</code> geeft het aantal milliseconden sinds 1 januari 1970; trek twee <code>getTime()</code>-waarden af om een tijdsverschil te berekenen."] },
    { h: "Datums tonen en vergelijken", points: ["<code>toLocaleDateString()</code> toont de datum in een lokaal leesbaar formaat (bv. <code>31/12/2025</code>); <code>toLocaleString()</code> toont datum én tijd.", "Het verschil in dagen reken je uit door het ms-verschil te delen door <code>1000 * 60 * 60 * 24</code> en af te ronden met <code>Math.floor</code>."] },
    { h: "Object literals", points: ["Een object literal staat tussen accolades: <code>let p = { voornaam: \"An\", leeftijd: 20 };</code> — eigenschap en waarde scheid je met een dubbele punt, eigenschappen onderling met een komma.", "Objecten mogen genest zijn: <code>adres: { gemeente: \"Brugge\" }</code> als eigenschap binnenin.", "Lees een eigenschap met punt-notatie (<code>p.voornaam</code>) of bracket-notatie (<code>p[\"voornaam\"]</code>); een onbestaande sleutel geeft <code>undefined</code>.", "Met <code>delete p.leeftijd</code> verwijder je een eigenschap, en met <code>for (const key in p)</code> overloop je alle sleutels."] },
    { h: "JSON: stringify & parse", points: ["<code>JSON.stringify(object)</code> zet een object of array om naar een JSON-string (tekst), bv. om het te bewaren of door te sturen.", "<code>JSON.parse(tekst)</code> doet het omgekeerde: van een JSON-string terug een echt JavaScript-object/array, zodat je de eigenschappen weer kunt aanspreken.", "In JSON staan eigenschap-namen altijd tussen dubbele aanhalingstekens en zit er enkel data in — geen functies of commentaar."] },
  ],
  labo21: [
    { h: "Wat is Web Storage?", points: ["Met de Web Storage API bewaar je sleutel/waarde-paren in de browser, afgebakend per domein.", "<code>localStorage</code> blijft bewaard, ook na het sluiten en heropenen van de browser.", "<code>sessionStorage</code> verdwijnt zodra je het tabblad of de browser sluit; voor de rest werken beide identiek."] },
    { h: "Methodes van storage", points: ["<code>setItem(sleutel, waarde)</code> bewaart een waarde onder een sleutel.", "<code>getItem(sleutel)</code> haalt ze weer op — let op: dit geeft <code>null</code> als de sleutel niet bestaat.", "<code>removeItem(sleutel)</code> wist net dat ene item; <code>clear()</code> wist alle items voor dit domein."] },
    { h: "Enkel strings: objecten via JSON", points: ["Web Storage bewaart enkel strings; een object of array kun je er niet rechtstreeks in steken.", "Bij het bewaren zet je daarom je object eerst om met <code>JSON.stringify(...)</code> en geef je die string aan <code>setItem</code>.", "Bij het inlezen haal je de string op met <code>getItem</code> en zet je ze terug om met <code>JSON.parse(...)</code>.", "Ook losse getallen worden als tekst bewaard; met <code>parseInt</code> of <code>Number</code> maak je er weer een getal van."] },
    { h: "Veilig inladen met een null-check", points: ["Omdat <code>getItem</code> <code>null</code> teruggeeft bij een onbestaande sleutel, moet je dat eerst controleren vóór je <code>JSON.parse</code> oproept.", "Een veilig patroon: <code>const raw = localStorage.getItem(\"data\"); const lijst = raw !== null ? JSON.parse(raw) : [];</code>.", "Zo vermijd je dat <code>JSON.parse(null)</code> een fout of een verkeerde waarde geeft."] },
    { h: "Unieke sleutels & valkuilen", points: ["Gebruik een unieke sleutel (bv. <code>\"VIVES.be.instellingen\"</code> in plaats van gewoon <code>\"data\"</code>) zodat een andere pagina op hetzelfde domein jouw waarde niet overschrijft.", "In een afgeschermde omgeving (sandbox/iframe) kan storage een fout gooien; wikkel de toegang dan in een <code>try</code>/<code>catch</code> zodat de rest van je script blijft werken."] },
  ],
  labo22: [
    { h: "Alles samen: het eindproject", points: ["De startpagina combineert alles uit de vorige labo's: DOM-manipulatie, events, arrays, objecten, <code>Date</code>, timers, JSON en (local)Storage.", "Het centrale patroon is: gegevens inladen uit storage, renderen naar de DOM, op events reageren, en bij elke wijziging opnieuw bewaren."] },
    { h: "Een tikkende klok", points: ["Voor een klok die elke seconde ververst gebruik je <code>setInterval(toonTijd, 1000)</code>, want <code>setInterval</code> herhaalt (terwijl <code>setTimeout</code> maar één keer uitvoert).", "In de functie haal je telkens een verse <code>new Date()</code> op en lees je <code>getHours()</code>, <code>getMinutes()</code> en <code>getSeconds()</code> uit.", "Roep de functie ook meteen één keer rechtstreeks op, zodat de klok niet eerst een seconde leeg blijft."] },
    { h: "Begroeting op basis van het uur", points: ["Met <code>new Date().getHours()</code> krijg je het uur (0 t/m 23) en kies je met <code>if</code>'s een passende begroeting.", "Werk van klein naar groot met aparte grenzen, bv. <code>if (uur &lt; 12) return \"Goedemorgen\";</code> daarna <code>if (uur &lt; 18) return \"Goedemiddag\";</code> en anders <code>return \"Goedenavond\";</code>.", "Door telkens te <code>return</code>en stopt de functie meteen bij de eerste passende grens."] },
    { h: "History als array van objecten", points: ["Je weet vooraf niet hoeveel commando's de gebruiker ingeeft, dus bewaar je ze in een array waar je er telkens één bijduwt met <code>push</code>.", "Elk commando is een object dat de bij elkaar horende gegevens bundelt, bv. <code>{ title, text, url }</code>.", "Met <code>forEach</code> overloop je de array om voor elk item een kaartje in de DOM te tekenen (met <code>createElement</code> en <code>appendChild</code>)."] },
    { h: "Bewaren, herladen en links openen", points: ["Bij elke wijziging bewaar je de hele array als JSON: <code>localStorage.setItem(\"history\", JSON.stringify(history))</code>.", "Bij het laden lees je terug in met een null-check: enkel <code>JSON.parse</code> als <code>getItem</code> niet <code>null</code> teruggaf.", "Met <code>window.open(url, \"_blank\")</code> open je de zoek-URL in een nieuw tabblad.", "Omdat storage en <code>window.open</code> in een sandbox geblokkeerd kunnen zijn, wikkel je ze in een <code>try</code>/<code>catch</code>."] },
  ],
};
