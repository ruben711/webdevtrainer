import { ComingSoon } from "@/components/ComingSoon";

export default function SandboxPage() {
  return (
    <ComingSoon title="Speeltuin" icon="beaker">
      Een vrije sandbox om HTML, CSS en JS te schrijven zonder opdracht — met
      dezelfde live preview en console als de oefeningen. Komt eraan zodra de
      runner volledig staat.
    </ComingSoon>
  );
}
